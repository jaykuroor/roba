You are implementing Track A: Demand & Sensing for the restaurant multi-agent demo.

Before coding, read and follow these files exactly:
- C:\Code\roba\docs\00_ARCHITECTURE.md
- C:\Code\roba\docs\01_TRACK_A.md

Assume the core architecture is already implemented
Inspect the repo structure and confirm the backend, frontend, Docker, and core files exist.

After core verification, implement Track A according to 01_TRACK_A.md:
- Python agents under track_a/
- Track A mocks under track_a/mocks/
- Track A tests under track_a/tests/
- React Track A panels under frontend/src/track_a/
- No business logic in the UI
- Use only relative frontend API/WS paths
- Preserve DEMO_MODE behavior exactly:
  - DEMO_MODE=track_a uses MockInventory
  - DEMO_MODE=combined disables mocks
- Track A may read core tables and signals, but must write only Track A-owned tables:
  forecasts, batches, competitor_offers, competitor_intel, review_insights
- Track A must never read/write Track B private tables or import Track B modules.

Use newly installed skills only where genuinely useful:
- Use computer-use / Browser when you need to control Brave Browser, inspect the live app, click through flows, or visually verify behavior.
- Use frontend-design and web-artifacts-builder when building or improving the React Track A panels. The core frontend design is not final; improve it where Track A needs better clarity, usability, layout, or visual hierarchy, while staying consistent with the existing shell.
- Use webapp-testing and playwright for frontend flow testing, interaction tests, and regression checks.
- Use screenshot when visual verification is useful for comparing UI states.
- Use security-best-practices if touching approval flows, agent call behavior, user inputs, API surfaces, or data boundaries.
- Use security-threat-model only if you identify meaningful cross-agent, approval, prompt-injection, or data-boundary risk.
- Use openai-docs only if LLM/provider behavior requires current official guidance.
- Use sentry/deploy skills only if the current task explicitly reaches monitoring or deployment.

Implementation priorities:
1. Inspect the existing implementation and determine what core contracts are already available.
2. Implement Track A incrementally, following 01_TRACK_A.md section by section.
3. Keep the architecture boundary strict: agents communicate via signals, not direct cross-agent calls.
4. Implement explainability everywhere required: forecasts and batch decisions must expose multipliers and human-readable event_log entries.
5. Improve Track A frontend UX where helpful:
   - forecast table/chart with clear multiplier breakdowns
   - batch decisions with readable reasons
   - competitor research flow with approval/call status
   - review insights with sentiment and suggested actions
   - staff coverage with station-level clarity
   - signal feed with expiry countdown and payload inspection
6. Add focused tests matching A9:
   - forecaster math, multipliers, clamp, batch truth table, explainability
   - competitor discovery and call outcome handling
   - review insight canned path, dedup, trend severity
   - staff coverage, cap, batch block, restore
   - contract tests for signal validation, group subscriptions, and no Track B imports
   - frontend tests for sample WS payload rendering and relative paths
7. Run backend and frontend tests.
8. Re-run the app with Docker Compose.
9. Use Brave Browser through Browser / Computer Use to visually test the implementation like an interactive feedback loop:
   - launch app
   - play simulation
   - inspect Forecast dashboard
   - trigger voice/text event if available
   - test call-in-sick flow
   - observe MockInventory menu toggle behavior in DEMO_MODE=track_a
   - inject or create a review
   - test competitor research approval/call flow if core supports it
   - verify SignalFeed updates
   - fix issues found, then retest

Do not stop at a plan unless blocked. Implement, run, visually test, fix, and repeat until Track A acceptance criteria are reasonably satisfied. At the end, summarize:
- files changed
- tests run
- Docker/Brave verification performed
- Track A acceptance criteria covered
- any remaining gaps or blocked items